import React from "react";

// --- PROPS RECEIVED ---
// PATTERNCONTAINER:
// all pattern info (title, userId, etc)

class Pattern extends React.Component {
  render() {
    return (
      <div>
        <h3>Pattern</h3>
      </div>
    );
  }
}

export default Pattern;
